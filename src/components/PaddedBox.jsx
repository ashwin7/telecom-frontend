import styled from '@emotion/styled';

const PaddedBox = styled.div`
  padding: 10; // Adjust padding as needed
  box-shadow: 2px 4px 6px rgba(0, 0, 0, 0.1); // Adjust shadow as needed
`;

export default PaddedBox;